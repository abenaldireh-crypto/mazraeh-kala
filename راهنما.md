# نصب — راه سادهٔ نهایی (بدون عکس)

## قدم ۱ — پاک‌سازی
اگر مخزن `mazraeh-kala` را قبلاً در گیت‌هاب ساختی، برو داخلش:
**Settings** (بالای صفحه مخزن) → پایین صفحه بیا → **Delete this repository** → طبق خواسته‌اش تایید کن.

این کار لازم است چون نسخهٔ قبلی یک پوشهٔ اضافه (`core`) داشت که مسیرها را خراب می‌کرد. با یک مخزن تازه، این مشکل اصلاً پیش نمی‌آید.

## قدم ۲ — مخزن تازه
در گیت‌هاب: **New** → اسم بگذار `mazraeh-kala` → **Create repository**

## قدم ۳ — یک آپلود، تمام
این زیپ را از حالت فشرده خارج کن. یک پوشه به اسم `ghfinal` می‌بینی. **بازش کن.**
داخلش دقیقاً ۴ چیز است:
- پوشهٔ `public`
- پوشهٔ `netlify`
- فایل `netlify.toml`
- فایل `راهنما.md`

هر ۴ تا را با هم انتخاب کن (کلیک روی اولی، بعد Shift و کلیک روی آخری) و همه را با هم بکش و در صفحهٔ آپلود گیت‌هاب رها کن.
**مهم:** پوشهٔ `ghfinal` را نکش — فقط چیزهای داخلش را.

پایین صفحه **Commit changes** را بزن. همین — فقط یک بار.

## قدم ۴ — وصل کردن به Netlify
برو داشبورد Netlify → سایت `mazraeh` → **Project configuration → Build & deploy**

اگر قبلاً مخزنی وصل شده بود، گزینهٔ تغییر/قطع اتصال را بزن و دوباره وصل کن؛ اگر نه، **Link repository** را بزن.
گیت‌هاب را انتخاب کن → مخزن `mazraeh-kala` را انتخاب کن.

وقتی صفحهٔ تنظیمات build باز شد:
- **Base directory:** خالی بگذار (اگر قبلاً `core` نوشته بودی، پاکش کن)
- **Publish directory:** بنویس `public`
- بقیه را دست نزن

**Deploy site** را بزن.

## قدم ۵ — رمز مدیریت
**Site configuration → Environment variables → Add a variable**
Key: `ADMIN_PASS` — Value: رمز دلخواهت

بعد: **Deploys → Trigger deploy → Clear cache and deploy site**

تمام. سایت را باز کن و امتحان کن.

---

اگر بعد از این هم خطا دید: به‌جای عکسِ خودِ سایت، وارد تب **Deploys** شو، روی آخرین دیپلوی بزن، و از صفحهٔ لاگ ساخت (که پر از متن سبز/قرمز است) اسکرین‌شات بگیر و بفرست. آن صفحه دقیقاً می‌گوید مشکل کجاست.
