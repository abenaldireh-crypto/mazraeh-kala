# نصب با GitHub Desktop — با عکس، بدون محدودیت فایل

## قدم ۱ — نصب GitHub Desktop
برو به `desktop.github.com` → دانلود → نصب معمولی (بعدی، بعدی).
بازش کن، با حساب گیت‌هاب خودت وارد شو.

## قدم ۲ — ساخت مخزن تازه
از منوی بالا: **File → New repository**
اسم: `mazraeh-kala`
محل ذخیره: هرجا دوست داری (مثلاً Desktop)
**Create repository**

## قدم ۳ — پیدا کردن پوشه
داخل GitHub Desktop دکمه‌ای هست به اسم **Show in Explorer** (ویندوز) یا **Show in Finder** (مک) — بزن. یک پوشهٔ خالی باز می‌شود.

## قدم ۴ — کپی فایل‌ها
این زیپ را باز کن. پوشهٔ `ghdesktop` را باز کن. سه چیز داخلش است:
- پوشهٔ `public` (شامل عکس‌ها هم هست)
- پوشهٔ `netlify`
- فایل `netlify.toml`

هر سه را کپی کن و در همان پوشهٔ خالیِ قدم ۳ پیست کن.

## قدم ۵ — ثبت و ارسال
برگرد به GitHub Desktop. پایین سمت چپ بنویس «آپلود اولیه با عکس» → **Commit to main**
بالای صفحه: **Publish repository**

چون تعداد فایل زیاد است (عکس‌ها) ممکن است یکی-دو دقیقه طول بکشد — صبر کن تا تمام شود.

## قدم ۶ — وصل به Netlify
Netlify → **Add new site → Import an existing project → GitHub** → مخزن `mazraeh-kala` را انتخاب کن.
Publish directory: بنویس `public`
**Deploy site**

## قدم ۷ — رمز مدیریت
**Site configuration → Environment variables** → `ADMIN_PASS` با رمز دلخواهت اضافه کن
بعد: **Deploys → Trigger deploy → Clear cache and deploy site**

## قدم ۸ — تست
لینک را باز کن، یک کالای کلار جستجو کن — باید هم قیمت هم عکس بیاید.

---

اگر خطا دید: از تب **Deploys** روی آخرین دیپلوی بزن، لاگ ساخت را اسکرین‌شات بگیر و بفرست.
